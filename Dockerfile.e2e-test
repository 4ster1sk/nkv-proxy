FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    pytest==8.3.4 \
    pytest-asyncio==0.25.0 \
    pytest-timeout==2.3.1 \
    httpx==0.28.1 \
    beautifulsoup4==4.12.3

CMD ["python", "-m", "pytest", "tests/e2e/", "-v", "-s", "--timeout=120"]
